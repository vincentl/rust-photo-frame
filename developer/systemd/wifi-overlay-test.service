[Unit]
Description=Wi‑Fi Overlay Takeover Test (dev)
After=greetd.service
Wants=greetd.service

[Service]
Type=oneshot
# Run as root; the script will drop to the kiosk user for sway via sudo -u kiosk
ExecStart=/usr/local/bin/overlay-test
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target

